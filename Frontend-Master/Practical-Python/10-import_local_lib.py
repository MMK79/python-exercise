import name_lib

my_name = "Fred"

my_name_length = name_lib.name_length(my_name)
my_name_lower_case = name_lib.lower_case_name(my_name)

print(f"in my code, my length is {my_name_length} and my lower case is {my_name_lower_case}")
